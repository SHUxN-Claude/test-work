# 引き継ぎメモ(年度推移管理ツール)

## 現在の状態

### 完成しているもの

| 対象 | 状態 |
|---|---|
| 単月ツール(`excel-tool/`) | **完成**。テンプレート + VBA 3本。Excel実機での動作確認は未 |
| 推移管理ブックの生成(`scripts/build_trend_workbook.py`) | **完成**。実PDFから推移表・グラフまで生成できる |
| 推移管理テンプレート(`trend-tool/`) | **完成**(シート・条件付き書式・グラフの骨組み) |
| デモ(`sample/デモ_推移管理_2026年度.xlsx`) | **完成**。4〜7月の推移が入った実物 |
| 推移管理ツールのVBA(`modTrend.bas`) | **未着手** ← 残作業 |

Python経路は今日から実用できます:

```bash
python scripts/build_trend_workbook.py -o 2026年度.xlsx \
  --pdf 4:4月.pdf --pdf 5:5月.pdf --pdf 6:6月.pdf --tol 20
```

## 残作業: `modTrend.bas` の実装

`excel-tool/vba/` の3本(modMain / modEngine / modEmbedded)はそのまま流用できます。
modEngine の `ParsePdf` がPDF1枚を12列の行データに変換するので、
推移管理ブック用には **13列目に「月」ラベル** を足して「全データ」へ追記する形になります。

実装すべきマクロは4本:

### 1. `月次取込(paths)`
- `ParsePdf` で読み、測定日から年度内月番号(4月=1〜3月=12)を決める
- **同じ月の既存行を「全データ」から削除してから追記**(取り直しの二重計上防止)
- 「取込履歴」に1行追記 → 最後に `再計算` を呼ぶ

### 2. `再計算()` ← 中核
Pythonの `group_points()` と同じ処理をVBAで書きます。

0. **先に手入力メモを退避する**(`read_carry()` 相当・順序が重要)
   - 「箇所メモ」シート(線別/キロ程/メモ)を読み込む = これが正本
   - 推移表の「メモ」列(H列)を走査し、**箇所メモにまだ無い文言だけ**を
     その行の代表キロ程で追加する
   - **自動表示された分を無条件に拾い直すとメモが増殖します**(実測済み)。
     再計算のたびに記録位置が増え、やがて無関係な箇所まで巻き込みます。
     「同じ線別・同じ文言が距離以内に既にあればスキップ」で止まります
   - 自動表示は複数メモを「 / 」で連結するので、分解してから判定すること
1. 設定シートから 目標値(C3)/基準値(C4)/黄色強調値(C5)/距離m(C6) を読む
2. 「全データ」を配列に読み込み、点の一覧を作る
   (線別・区分・キロ程・値・月番号・備考。区分は上下動/左右動のどちらに値があるかで決まる)
3. **線別+区分ごと**にキロ程順で並べ、隣との差が距離以内なら同じ箇所に連結(鎖方式)
   - 文字列キー `線別|区分|Format(km,"0000.000")` でソートすると簡単です
4. 「推移表」をクリアして書き出し(箇所Noはキロ程順の通し番号を毎回振り直す)
   - 月の値 = その箇所・その月の**最大値**
   - 取込済みの月に検出がなければ **「−」**(0.15G未満に収まっていた意味)
   - 代表キロ程 = **その箇所の最大値が出たときのキロ程**(月をまたいでも固定)
5. 「施工記録」を線別+キロ程で照合し、範囲±距離に入る箇所へ施工日・内容を転記
   同様に退避したメモも照合し「メモ」列(H列)へ復元。複数該当したら「 / 」で連結
   (併合でどちらのメモも失わないため)。最後に「箇所メモ」シートを書き直す
6. 「前年度実績」をキロ程で照合し「前年度値」列へ転記
7. 「①基準値超過」「②目標値超過」を作り直す(全月分)
8. `グラフ更新` を呼ぶ

> **重要な設計**: グループ分けは保存せず毎回すべて計算し直します。
> これにより距離設定を変えると過去の月も含めて組み替わります(ユーザーの要望)。
> 施工記録を推移表に直接書かずキロ程で持つのも、組み替えで行がズレないためです。

### 3. `グラフ更新()`
グラフは固定行を参照済みなので、値を書き込むだけで切り替わります。

- 41行B〜M列: 「グラフ」C2で選ばれた箇所Noの4月〜3月の値、A41に系列名
- 42行/43行: 目標値・基準値(水平線用に12列とも同じ値)
- 51〜55行: 最新月の値が大きい順に5箇所、A列に系列名
- 56行: 基準値

`グラフ` シートの `Worksheet_Change` でC2変更時に自動更新すると親切です。

### 4. `新年度開始()`
1. 確認ダイアログ(この操作は元に戻せない)
2. 「推移表」の内容を「前年度実績」へコピー
3. 「全データ」「①」「②」「取込履歴」「推移表」の月データをクリア
4. 設定シートの年度(C7)を+1

## シート仕様(実装時の参照用)

- データ系シート(全データ/①/②): 見出し4行目、データ5行目〜、13列
  `NO / 列車速度 / 時刻 / キロ程 / 上下動 / 左右動 / 超過印 / 備考 / 測定日 / 線別 / センター / 元ファイル / 月`
- 推移表: 見出し4行目、データ5行目〜、24列
  `箇所No / 線別 / 区分 / 代表キロ程 / 範囲(自) / 範囲(至) / 備考(PDF自動) / メモ(手入力=H列) / 4月…3月(I〜T列) / 年間最大(U) / 前年度値(V) / 施工日(W) / 施工内容(X)`
  ※ H列「メモ」はユーザーが直接入力する欄(うすい橙)。**クリア後に必ず復元すること**
- 箇所メモ: 見出し4行目、データ5行目〜。B=線別 / C=キロ程 / D=メモ
- 設定: C3目標値 / C4基準値 / C5黄色強調値 / C6距離(m) / C7年度
- 名前定義: `目標値` `基準値` `黄色強調値` (条件付き書式が参照。**消さないこと**)

## 未確認事項

- **VBAはExcel実機で未検証**です(この環境はLinuxのためExcelを動かせません)。
  単月ツールも含め、実機での動作確認が必要です。
- PDFは0.15G以上のみ掲載される様式と推定しています(6月データの最小値が0.15)。
  この前提が違うと「−」の意味が変わるので、ユーザーに確認してください。
- 同一箇所の距離は暫定20m。現場感覚での妥当性は要確認。
